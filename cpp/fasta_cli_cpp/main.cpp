#include <iostream>
#include "fasta.hpp"
#include <fstream>

BioCpp::Fasta BioCpp::from_file(std::string filename) {
  std::ifstream fasta_file;
  fasta_file.open (filename);
  if (fasta_file.is_open()) {
    
  }
}

int main() {
  
}
